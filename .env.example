# ── Database ──────────────────────────────────────────────────────────────────
# Local (Prisma dev server):
# DATABASE_URL="postgres://postgres:postgres@localhost:51214/template1?sslmode=disable"
# Production (Neon / Supabase / any Postgres):
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# ── Auth ──────────────────────────────────────────────────────────────────────
# Generate with: openssl rand -hex 32
JWT_SECRET="c0557eb8c2e755a7ff2b0e91f375967c2c4ed817e992353ab6eeae597e96fbe9"

# ── Encryption (SMTP passwords at rest) ──────────────────────────────────────
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="1976e69b7fdbe9a28b5c9bcea6da3403fe874e5dad018b87a399cd6878711e6b"

# ── Admin seed user ───────────────────────────────────────────────────────────
# Used by /api/auth/seed to create the first admin account
ADMIN_EMAIL="admin@codevisionaryservices.com"
ADMIN_PASSWORD="CVS@rocks"

# ── App URL (required for tracking pixel & unsubscribe links in emails) ───────
NEXT_PUBLIC_BASE_URL="https://coldreach-five.vercel.app"

# ── CAN-SPAM physical address (optional — can also be set via Settings page) ──
NEXT_PUBLIC_CAN_SPAM_ADDRESS="Your Company\n123 Street\nCity, State ZIP"

# ── OpenAI (optional — enables AI reply classification in leads) ──────────────
# OPENAI_API_KEY="sk-..."
